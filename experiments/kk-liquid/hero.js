const video=document.querySelector('#ring-video'),canvas=document.querySelector('#ring-canvas'),ctx=canvas.getContext('2d',{willReadFrequently:true}),reduce=matchMedia('(prefers-reduced-motion: reduce)');
let visible=true,last=-1;
video.muted=true;video.defaultMuted=true;
function draw(){if(video.readyState<2)return;ctx.drawImage(video,0,0,640,360);const frame=ctx.getImageData(0,0,640,360),p=frame.data;for(let i=0;i<p.length;i+=4){const r=p[i],g=p[i+1],b=p[i+2],chroma=r-Math.max(g,b);let a=Math.max(0,Math.min(1,(chroma-12)/34));/* Remove neutral studio background and floor shadow; retain orange paint. */const px=(i/4)%640,py=Math.floor(i/2560);if(((px-320)/133)**2+((py-180)/132)**2<1)a=1;if(py>320)a*=Math.max(0,1-(py-320)/6);p[i+3]=Math.round(a*255);}ctx.putImageData(frame,0,0);canvas.dataset.ready='true';}
function play(){if(visible&&!document.hidden&&!reduce.matches)video.play().catch(()=>{});else video.pause();}
video.addEventListener('loadeddata',()=>{draw();play()});video.addEventListener('seeked',draw);video.addEventListener('error',()=>{canvas.dataset.error='video-unavailable'});reduce.addEventListener('change',play);document.addEventListener('visibilitychange',play);new IntersectionObserver(e=>{visible=e[0].isIntersecting;play()}).observe(canvas);
document.addEventListener('pointerdown',play,{passive:true});
function update(){if(visible&&!document.hidden&&video.currentTime!==last){last=video.currentTime;draw();}requestAnimationFrame(update)}requestAnimationFrame(update);play();